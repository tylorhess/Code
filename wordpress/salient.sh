Salient
	General Settings
		upload favicon
		"back to top" button on pages?
		smooth scrolling?
		enables responsive design?
		global call to action text
		gloabl call to action button text
		(global?) call to action link
		pages to exclude call to action from
		google analytics
		custom css
	Color Options
		Accent Color, Extra Color 1, Extra Color 2, Extra Color 3
	Boxed Layout Options
		enables boxed layout = NOT full-width = less than full width with background color, texture, or image
		background color
		background image
		background repeat
		background position
		background scroll (normal) or fixed (not move with scroll)
		auto-resize background image to fit window?
	Typography Options
		use custom fonts?
			body, nav, nav dropdown, slider caption & testimonial, standard header, sidebar carousel button & footer headers, sub-headers & team members names
			include/exclude latin characters?
	Header Options
		header color light/dark?
		logo as image or text?
		header padding # i.e. - 28px
		header resize on scroll
		header layout = standard header | header with secondary nav # "header with secondary nav" turns on: Appearance > Menus > Secondary Navigation Menu
		enables social icons? # only with secondary nav 
	Footer Options
		On/Off Widgetized (Main) Footer? # enables/disables top, widgetized footer 
		Copywrite Section # bottom (copywrite) footer always enabled
			copywrite section text
			check/uncheck social icons: facebook, twitter, google+, ...
	Home Options
		On/Off slider caption transition animations
		On/Off slider image auto-resize to screen size
		On/Off autoplay slider
		slider advance speed (default: 5500 ms)
		slider animation speed (default: 800 ms)
		slider height (400px min, 650px used with demo)
		slider background color (only seen when image is loading or too small)



Appearance
	Menus
		Top Navigation Menu			# default (always enabled)
		Secondary Navigation Menu	# optional (turned on here: Salient > Header Options > Header Layout > Header with Secondary Navigation)

########## break out of .container ##########
(Text tab)
<div class="carousel-wrap" data-full-width="true">
	...
</div>

# button
[button color="Accent-Color" size="small" url="#" text="UNLOCK"] 
<a class="nectar-button small accent-color" href="#">UNLOCK</a>